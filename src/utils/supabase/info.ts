export const supabaseInfo = {
  projectName: 'Braj Darshan',
  authEnabled: true,
  db: 'PostgreSQL',
 
};

export const projectId = process.env.projectId
export const publicAnonKey = process.env.publicAnonKey